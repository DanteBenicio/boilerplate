import { render, screen } from '@testing-library/react';
import {{pascalCase name}} from '.';

describe('<{{pascalCase name}} />', () => {
  it('should render heading of the {{pascalCase name}} Component', () => {
    render(<{{pascalCase name}} />);

    expect(screen.getByRole('heading', { name: /{{pascalCase name}}/i })).toBeInTheDocument();
  });

  it('should take a snapshot from {{pascalCase name}} component', () => {
    const { container } = render(<{{pascalCase name}} />);

    expect(container.firstChild).toMatchSnapshot();
  });
});
